# هيكل المشروع

## الملفات الأساسية

### `main.py`
الملف الرئيسي للبوت - يحتوي على:
- اتصال بديسكورد
- دالة إنشاء الأخبار باستخدام OpenAI API
- جدولة إرسال الأخبار كل 6 ساعات
- أمر `!وريني` للاختبار

### `requirements.txt`
المكتبات المطلوبة:
- `discord.py==2.3.2` - مكتبة ديسكورد
- `python-dotenv==1.0.0` - لقراءة متغيرات البيئة
- `aiohttp==3.9.1` - للطلبات غير المتزامنة

### `.env.example`
نموذج لملف البيئة - انسخه إلى `.env` واملأه بالتوكنات

## ملفات Railway

### `Procfile`
يحدد أمر بدء تشغيل البوت على Railway:
```
worker: python main.py
```

### `runtime.txt`
يحدد إصدار Python المستخدم:
```
python-3.11.7
```

### `nixpacks.toml`
إعدادات Nixpacks للبناء على Railway

### `railway.json`
إعدادات إضافية لـ Railway

## ملفات التوثيق

### `README.md`
دليل استخدام المشروع الأساسي

### `SETUP.md`
دليل إعداد شامل خطوة بخطوة

### `PROJECT_STRUCTURE.md`
هذا الملف - يوضح هيكل المشروع

## ملفات مساعدة

### `check_setup.py`
سكريبت للتحقق من صحة الإعداد قبل التشغيل

### `.gitignore`
ملفات يجب تجاهلها في Git

### `.dockerignore`
ملفات يجب تجاهلها في Docker (إن استُخدم)

## كيفية الاستخدام

1. **للاختبار المحلي:**
   ```bash
   python check_setup.py  # للتحقق من الإعداد
   python main.py         # لتشغيل البوت
   ```

2. **للرفع على Railway:**
   - ارفع الكود على GitHub
   - اربط مع Railway
   - أضف متغيرات البيئة
   - البوت سيبدأ تلقائياً

## ملاحظات

- تأكد من تفعيل `MESSAGE_CONTENT_INTENT` في Discord Developer Portal
- القناة المستهدفة يجب أن تحتوي على `「📰」cyber-news` في اسمها
- البوت يرسل الأخبار كل 6 ساعات تلقائياً

