# بوت الأخبار السيبرانية لديسكورد

بوت ديسكورد يرسل أخبار سيبرانية تلقائياً كل 6 ساعات باستخدام الذكاء الاصطناعي.

## المميزات

- 📰 إرسال أخبار سيبرانية تلقائياً كل 6 ساعات
- 🤖 استخدام OpenAI API لإنشاء الأخبار
- ⚡ أمر `وريني` لإرسال خبر فوري للاختبار
- 🚀 جاهز للرفع على Railway

## الإعداد

1. قم بتثبيت المتطلبات:
```bash
pip install -r requirements.txt
```

2. أنشئ ملف `.env` واملأه:
```
DISCORD_TOKEN=your_discord_bot_token_here
OPENAI_API_KEY=your_openai_api_key_here
```

3. شغّل البوت:
```bash
python main.py
```

## الرفع على Railway

### الخطوات:

1. **ارفع الكود على GitHub:**
   ```bash
   git init
   git add .
   git commit -m "Initial commit"
   git remote add origin <your-repo-url>
   git push -u origin main
   ```

2. **اربط المستودع مع Railway:**
   - اذهب إلى [Railway](https://railway.app)
   - اضغط على "New Project"
   - اختر "Deploy from GitHub repo"
   - اختر المستودع الخاص بك

3. **أضف متغيرات البيئة (Environment Variables):**
   - في صفحة المشروع، اضغط على "Variables"
   - أضف المتغيرات التالية:
     - `DISCORD_TOKEN` = توكن البوت من [Discord Developer Portal](https://discord.com/developers/applications)
     - `OPENAI_API_KEY` = مفتاح API من [OpenAI](https://platform.openai.com/api-keys)

4. **تأكد من الإعدادات:**
   - Railway سيكتشف تلقائياً أن هذا مشروع Python
   - تأكد من أن `Procfile` موجود (موجود بالفعل)
   - البوت سيبدأ تلقائياً بعد الرفع

### ملاحظات مهمة:
- تأكد من أن البوت لديه صلاحيات `MESSAGE_CONTENT_INTENT` في Discord Developer Portal
- تأكد من أن البوت عضو في السيرفر ولديه صلاحيات القراءة والكتابة
- القناة يجب أن تحتوي على `「📰」cyber-news` في اسمها

## الأوامر

- `!وريني` - إرسال خبر سيبراني فوري

## ملاحظات

- تأكد من أن البوت لديه صلاحيات القراءة والكتابة في القناة
- القناة المستهدفة: `「📰」cyber-news`
- البوت يرسل الأخبار كل 6 ساعات تلقائياً

