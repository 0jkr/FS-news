# دليل الإعداد الكامل

## 1. إنشاء بوت ديسكورد

1. اذهب إلى [Discord Developer Portal](https://discord.com/developers/applications)
2. اضغط على "New Application" وأعطها اسماً
3. اذهب إلى "Bot" في القائمة الجانبية
4. اضغط على "Add Bot"
5. في قسم "Privileged Gateway Intents"، فعّل:
   - ✅ MESSAGE CONTENT INTENT (مهم جداً!)
6. اضغط على "Reset Token" وانسخ التوكن
7. اذهب إلى "OAuth2" > "URL Generator"
8. اختر:
   - Scopes: `bot`
   - Bot Permissions: `Send Messages`, `Read Message History`, `Embed Links`
9. افتح الرابط وأضف البوت إلى سيرفرك

## 2. الحصول على OpenAI API Key

1. اذهب إلى [OpenAI Platform](https://platform.openai.com)
2. سجّل دخول أو أنشئ حساب
3. اذهب إلى [API Keys](https://platform.openai.com/api-keys)
4. اضغط على "Create new secret key"
5. انسخ المفتاح (سيظهر مرة واحدة فقط!)

## 3. الإعداد المحلي

1. أنشئ ملف `.env`:
   ```bash
   cp .env.example .env
   ```

2. عدّل ملف `.env` وأضف التوكنات:
   ```
   DISCORD_TOKEN=your_discord_bot_token_here
   OPENAI_API_KEY=your_openai_api_key_here
   ```

3. ثبّت المتطلبات:
   ```bash
   pip install -r requirements.txt
   ```

4. شغّل البوت:
   ```bash
   python main.py
   ```

## 4. إنشاء القناة

1. في سيرفر ديسكورد، أنشئ قناة جديدة
2. اسم القناة يجب أن يحتوي على: `「📰」cyber-news`
3. تأكد من أن البوت لديه صلاحيات الكتابة في القناة

## 5. اختبار البوت

اكتب في أي قناة:
```
!وريني
```

يجب أن يرسل البوت خبراً سيبرانياً فوراً.

## 6. الرفع على Railway

اتبع التعليمات في `README.md` في قسم "الرفع على Railway"

## استكشاف الأخطاء

### البوت لا يستجيب للأوامر
- تأكد من تفعيل `MESSAGE_CONTENT_INTENT` في Discord Developer Portal
- تأكد من أن البوت عضو في السيرفر

### خطأ في OpenAI API
- تأكد من أن المفتاح صحيح
- تأكد من أن لديك رصيد في حساب OpenAI

### البوت لا يرسل الأخبار التلقائية
- تأكد من أن القناة موجودة واسمها يحتوي على `「📰」cyber-news`
- تأكد من أن البوت لديه صلاحيات الكتابة

